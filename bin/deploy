#!/bin/sh
set -e

cd "$(dirname "$0")/.."

echo "Stopping falcon..."
docker compose down

echo "Building..."
docker compose build

echo "Starting falcon..."
docker compose up -d

echo "Done. Logs: docker compose logs -f falcon"
